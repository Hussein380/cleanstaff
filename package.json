{
    "name": "cleanstaff-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "build": "cd frontend && npm ci && npm rebuild && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}